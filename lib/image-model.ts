/**
 * Keep the image-model selection in one place so generation, local edits and
 * the layers experiment always use the same rendering engine.
 */
export const IMAGE_MODEL = "gpt-image-2.5-sunburst";

